<nav class="header__nav">
    <div class="mobile-menu-toggle">
        <button id="menu-toggle" aria-label="Toggle menu">
            &#9776;
        </button>
    </div>

    <a class="header__title" href="{{ path('app_home') }}">
        Auto<span class="color-primary-red">Mobiles</span>
    </a>
    <script>
    document.addEventListener('DOMContentLoaded', function () {
        const toggleBtn = document.getElementById('menu-toggle');
        const menu = document.getElementById('mobile-menu');

        if (toggleBtn && menu) {
            toggleBtn.addEventListener('click', function () {
                menu.classList.toggle('open');
            });
        }
    });
</script>

    <div class="menu" id="mobile-menu">
        <ul class="header__nav_ul">
            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('account_index') }}">Mon compte</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_register_index') }}">Inscription</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
